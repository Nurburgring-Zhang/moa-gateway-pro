{
  "name": "moa-gateway-mobile",
  "version": "3.1.1",
  "description": "MoA Gateway Pro — Android client (Capacitor). Wraps the admin/orchestration web UI and connects to a deployed MoA gateway.",
  "license": "Apache-2.0",
  "scripts": {
    "sync": "cap sync android",
    "open": "cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
