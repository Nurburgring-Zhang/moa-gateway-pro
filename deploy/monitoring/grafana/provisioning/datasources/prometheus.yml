# Grafana Prometheus datasource (v3.2.1) — required for the dashboard panels
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
