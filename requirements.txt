# MoA Gateway Pro — 工业级多模型协作网关
# 生产环境依赖清单。
# audit F1 修复: 核心框架锁定到已验证通过的运行时版本(与 venv 实测一致),
# 消除"声明版本"与"实际运行版本"漂移。其余给出已验证的下限区间。

# Web 服务 (verified runtime versions)
fastapi==0.141.1
uvicorn[standard]>=0.30.0,<1.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart>=0.0.18

# HTTP 客户端 (verified)
httpx==0.28.1
aiohttp>=3.14.3
websockets>=13.1

# 数据 / 存储 (verified)
sqlalchemy==2.0.35
aiosqlite>=0.20.0

# 安全 / 认证 (verified)
python-jose[cryptography]>=3.4.0   # JWT - SEC-003: CVE-2024-33663
bcrypt>=4.1.0                       # 密码哈希 (直接使用,非passlib)
cryptography>=43.0.1                # AES-256-GCM 加密 (verified on 50.x)

# 工具 (verified lower bounds)
PyYAML>=6.0.2
rich>=13.9.4
typer>=0.12.5
tenacity>=9.0.0
psutil>=6.1.0
jinja2>=3.1.0

# 可观测性 (生产必需)
prometheus_client>=0.20.0

# 分布式缓存 (生产推荐)
redis>=5.0.0

# Web search providers (可选,按需启用)
tavily-python>=0.5.0
duckduckgo-search>=6.0.0

# Audio editing (可选)
elevenlabs>=1.0.0

# 3D mesh processing (可选)
trimesh>=4.0.0
