# MoA Gateway Pro — 工业级多模型协作网关
# 依赖清单。锁定主版本,避免大版本兼容性问题。

# Web 服务
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12

# HTTP 客户端
httpx==0.27.2
aiohttp==3.10.10
websockets==13.1

# 数据 / 存储
sqlalchemy==2.0.35
aiosqlite==0.20.0

# 工具
PyYAML==6.0.2
rich==13.9.4            # CLI 输出美化
typer==0.12.5           # CLI 工具
tenacity==9.0.0         # 重试退避
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4  # 密码哈希
cryptography==43.0.1    # 加密
psutil==7.2.2           # 系统监控

# 可选:向量/缓存(若要启用语义缓存)
# numpy==2.1.2
# faiss-cpu==1.9.0.post1

# 测试
pytest==8.3.3
pytest-asyncio==0.24.0
httpx>=0.27
# Desktop UI(基于 flet,Flutter for Python,自带 Skia 渲染)
flet>=0.27
